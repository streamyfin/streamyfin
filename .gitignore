# Dependencies and Package Managers
node_modules/
bun.lockb
package-lock.json

# Expo and React Native Build Artifacts
.expo/
dist/
dist-web/
desktop/
web-build/
.tsbuildinfo

# Platform-specific Build Directories
/ios
/android

# Gradle caches (top-level + per-module native projects)
**/.gradle/

# Native module build outputs (any module)
modules/*/android/build/

# Generated Applications
Streamyfin.app
*.apk
*.ipa
*.aab

# Certificates and Keys
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Debug and Temporary Files
npm-debug.*
*.orig.*
*.mp4

# OS-specific Files
# macOS
.DS_Store

# IDE and Editor Files
.vscode/
.idea/
.ruby-lsp
.cursor/

# Environment and Configuration
expo-env.d.ts
.continuerc.json
.env
.env.local

# Secrets and Credentials
pc-api-7079014811501811218-719-3b9f15aeccf8.json
credentials.json
streamyfin-4fec1-firebase-adminsdk.json
/profiles/
certs/

# Version and Backup Files
/version-backup-*

# ios:unsigned-build Artifacts
build/
# but keep EAS custom build configs (the generic build/ rule above matches .eas/build/)
!.eas/build/
!.eas/build/**
# Jest coverage reports
coverage/

# Claude Code: the shared conventions are tracked so a new learned fact or agent
# reaches the repo; everything else under .claude/ is per-machine state.
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/learned-facts/
!.claude/learned-facts.md
.agents/skills/**
skills-lock.json

# CI-injected Google Play service account key (written at build time)
google-service-account.json
