apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.systemvolume'
version = '1.0.0'

expoModule {
  canBePublished false
}

android {
  namespace "expo.modules.systemvolume"
  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }
}

dependencies {
}
